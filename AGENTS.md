# AGENTS.md — 学习知识库

## 项目概述

个人 AI/LLM/Agent 学习知识库，目标是三个月内达到专家级水平。

### 核心模块

| 模块 | 定位 | 覆盖内容 |
|------|------|----------|
| AI 架构师知识库 | 系统架构层面 | LLM理论、模型工程化、RAG架构、Agent系统、LLMOps |
| AI 应用工程师知识库 | 应用开发层面 | API集成、Prompt工程、RAG实战、Agent开发、框架选型 |
| AI 数学知识库 | 理论基础 | 线性代数、微积分、概率论、信息论、最优化 |
| 大模型训练及 Agent 知识库 | 训练与Agent | 预训练、微调、评估、Agent架构、多Agent系统 |
| 计算机数学知识库 | CS数学 | 离散数学、算法、数据结构 |
| 量子计算知识库 | 前沿探索 | 量子基础、算法、架构、应用 |
| 架构与云原生知识库 | 基础设施 | 基础架构、软件架构、云原生 |
| AI 实战 | 实战笔记 | 单Agent、多Agent、知识库系统性总结 |
| AI 项目 | 项目方案 | 多Agent API调用系统设计方案与实现 |

### 技术栈

- **文档**: Markdown 为主（95%+），少量 `.docx`（导出产物）
- **代码**: Python 2 脚本（matplotlib/numpy 可视化）、HTML 2 页面（AI 架构师官网）
- **设计文档示例**: TypeScript / Python 代码片段
- **无构建系统、无测试框架、无 CI/CD**

## 构建 / 测试 / Lint 命令

本仓库没有构建系统或测试框架。以下为可用命令：

```bash
# Python 可视化脚本 — 需要 numpy + matplotlib
pip install numpy matplotlib
python "AI数学知识库/visualizations/generate_visualizations.py"
python "AI数学知识库/visualizations/generate_svg.py"

# Markdown 格式检查（如已安装）
npx markdownlint-cli2 "**/*.md" --fix

# 查看 Python 语法检查（需安装 ruff 或 flake8）
pip install ruff
ruff check AI数学知识库/visualizations/
```

## 代码风格

### Markdown 文档

- **标题**: `#` 一级标题，`##` 章节，`###` 小节
- **文件名**: 中文 + 数字前缀（如 `01-Transformer架构详解.md`），前缀用于排序
- **目录结构**: 每个知识库用 `index.md` 或 `README.md` 做入口
- **表格**: 用于结构对比，如架构对比、工具选型
- **代码块**: 必须标注语言（` ```python `、` ```typescript `、` ```bash `）
- **Emoji 标注**: 🔥 前沿/新内容，✅ 已完成，⚠️ 注意
- **内联代码**: 反引号包裹文件名、变量名、命令
- **分隔线**: `---` 分隔大段
- **导出注意**: `.docx` 文件不修改，需要时重新从 .md 导出
- **引文块**: `>` 用于核心观点或摘要

### Python

- **命名**: `snake_case` 函数/变量，`PascalCase` 类，`UPPER_CASE` 常量
- **导入顺序**: 标准库 → 第三方库（numpy, matplotlib），每组空行分隔
- **类型标注**: 函数参数和返回值尽量加类型提示（`def func(x: int) -> str:`）
- **文档字符串**: 模块级和函数级用 `""" ... """`，说明功能而非实现细节
- **字符串**: 优先 f-string，避免 `%` 格式化或 `+` 拼接
- **入口**: `if __name__ == "__main__":` 保护脚本执行入口
- **注释**: 中文注释解释"为什么"，英文代码标识符；注释不写显而易见的内容
- **错误处理**: try/except 具体异常类型，避免裸 `except:`；精确捕获 `ValueError`、`FileNotFoundError` 等
- **Matplotlib 规范**: 使用 `COLORS` 常量字典统一配色；保存时 `dpi=300`、`bbox_inches='tight'`；`plt.close(fig)` 释放内存
- **文件头**: 模块级 docstring 说明用途，如 `"""AI数学知识库可视化生成器"""`
- **行长度**: 不超过 100 字符

### TypeScript / JavaScript

- **类型**: 优先 `interface` 而非 `type`，`enum` 用于枚举
- **命名**: `PascalCase` 类/接口/枚举，`camelCase` 函数/变量/方法
- **异步**: `async/await`，避免裸 `.then()`
- **可选链**: `?.` 和 `??` 优先于 `&&` 短路
- **模板**: 模板字符串反引号替代字符串拼接
- **集合**: `Map<K, V>` 替代对象作为字典
- **注释**: JSDoc `/** */` 用于公开 API 注释

### 目录结构约定

```
知识库名称/
├── index.md                # 入口/目录
├── 01-主题/                # 数字前缀保证排序
│   ├── 01-文件.md
│   └── 02-文件.md
├── 02-主题/
└── appendices/             # 附录
```

- 目录名：中文 + 数字前缀
- 每个知识库一个根目录，平铺在仓库根下
- `.md` 为主，`.docx` 为导出产物（不直接编辑）

### 知识库编写规范

- **每个文档必须有清晰的受众和前置知识说明**（如 "目标读者：AI应用开发者，前置：熟悉Python"）
- **复杂概念必须附代码示例或伪代码**，示例代码保持可运行性
- **对比类内容用表格**，流程类内容用列表或图表
- **代码模板库**: `AI应用工程师知识库/08-代码模板库.md` 集中存放可复用模板
- **抽象层级**: 每个概念从"直观理解 → 数学形式 → 代码实现"三层递进
- **参考文献和进一步阅读链接放在文末**，标注推荐指数
- **反模式/常见错误单独成节**（如 "常见陷阱"、"反模式"）
- **检查清单放在章节末尾**，便于自查
- **索引/目录文件**: 每个知识库根目录的 `index.md` 或 `README.md` 维护最新目录

### 学习管理规范

- **学习计划**: `.docx` 格式，源自 `.md` 导出
- **进度报告**: `学习进度报告.md` 定期更新
- **Commit 消息**: 中文，简洁描述改动内容，无需 conventional commit 格式

### Git 工作流

- 直接提交到 main 分支
- Commit 用中文，一行内描述改动
- 示例: `更新知识覆盖顶尖及前沿技术`
- 大量重组时用 `增加项目文件` 类宽泛描述

## AI Agent 操作指南

### 通用原则

- **先读后写**: 修改文件前先读完整内容，理解上下文后再编辑
- **保持一致性**: 新增内容遵循现有文档的风格和结构
- **不破坏中文编码**: 所有 `.md` 文件 UTF-8 编码，注意中英文混排空格
- **不编辑 .docx**: docx 为导出产物，需要时从 .md 源文件重新生成
- **模块化更新**: 更新 `学习进度报告.md` 时要同步反映各模块最新状态

### 常见任务

1. **创建新知识库**: 根目录新建文件夹 → 创建 `index.md` 入口 → 数字前缀子目录
2. **新增章节**: 找到对应知识库 → 数字前缀顺序插入 → 更新 `index.md`
3. **更新学习进度**: 修改 `学习进度报告.md`，更新目标模块的完成状态和日期
4. **添加可视化**: Python 脚本放在 `AI数学知识库/visualizations/`，遵循现有配色和保存规范
5. **修复知识错误**: 找到原文档 → 确认上下文 → 精确替换错误内容

### 学习路线图

根据现有知识库结构，推荐的学习路径（三个月）：

| 阶段 | 时间 | 重点模块 | 目标 |
|------|------|----------|------|
| 基础 | 第1-2周 | AI数学知识库 + AI应用工程师知识库 | 夯实数学基础，掌握LLM应用开发 |
| 进阶 | 第3-6周 | AI架构师知识库 + 大模型训练及Agent知识库 | 理解架构设计，掌握Agent系统 |
| 实战 | 第7-10周 | AI实战 + AI项目 + 架构与云原生 | 动手实践，构建完整项目 |
| 前沿 | 第11-12周 | 量子计算 + 前沿技术跟踪 | 拓展视野，形成专家级知识体系 |
